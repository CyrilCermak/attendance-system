<p id="notice"><%= notice %></p>

<h1>Listing Warkers</h1>

<table>
  <thead>
    <tr>
      <th>Mac</th>
      <th>First name</th>
      <th>Ip</th>
      <th>Mac</th>
      <th>State</th>
      <th>Last name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @workers.each do |worker| %>
      <tr>
        <td><%= worker.mac %></td>
        <td><%= worker.first_name %></td>
        <td><%= worker.ip %></td>
        <td><%= worker.mac %></td>
        <td><%= worker.state %></td>
        <td><%= worker.last_name %></td>
        <td><%= link_to 'Show', worker %></td>
        <td><%= link_to 'Edit', edit_worker_path(worker) %></td>
        <td><%= link_to 'Destroy', worker, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Warker', new_worker_path %>
